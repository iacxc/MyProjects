#!/bin/sh -x

host=localhost
#Desc: list files with a specific user:it3
#      but this can pass if ranger permission opened
curl "http://$host:50070/webhdfs/v1/user/caiche?op=LISTSTATUS&user.name=it3" 
