#!/bin/sh -x

#Desc: from knox, pass knox authentication & authorization, refused by hdfs acl
curl --noproxy localhost -u guest:guest-password -k "https://localhost:8443/gateway/default/webhdfs/v1/user/caiche?op=LISTSTATUS" | json_pp
