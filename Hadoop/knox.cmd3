#!/bin/sh -x

#Desc: from knox, pass knox authentication & authorization, refused by hdfs acl
curl -u guest:guest-password -k "https://localhost:8443/gateway/default/webhdfs/v1/user/caiche?op=LISTSTATUS" 
